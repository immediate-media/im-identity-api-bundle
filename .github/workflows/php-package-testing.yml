name: PHP Package Tests
on:
  pull_request:
    branches:
      - main

jobs:
  call-workflow:
    uses: immediate-media/github-actions/.github/workflows/php-package-testing.yml@feature-php-workflow
    with:
      php-version: '8.2'
